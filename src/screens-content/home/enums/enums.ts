export enum ImageLayout {
  FIXED = "fixed",
  FILL = "fill",
  INTRINSIC = "intrinsic"
}

export enum TextAlign {
  RIGHT = "right",
}

export enum ObjectFit {
  CONTAIN = "contain",
  COVER = "cover",
}
